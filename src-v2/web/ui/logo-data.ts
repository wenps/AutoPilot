/**
 * AutoPilot Logo — 使用 assets/logo/contours (2).svg 的 SVG data URI。
 */
const LOGO_SVG = `<svg xmlns="http://www.w3.org/2000/svg" width="360" height="348" viewBox="0 0 360 348" preserveAspectRatio="xMinYMin meet"></svg>`;

export const LOGO_DATA_URL = `data:image/svg+xml;utf8,${encodeURIComponent(LOGO_SVG)}`;
